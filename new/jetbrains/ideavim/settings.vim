" # Search

" Highlight matches with the last search pattern
set hlsearch

" Highlight where search pattern typed so far matches
set incsearch

" Ignore case in search patterns
set ignorecase

" Use case sensitive search if any character in the pattern is uppercase
set smartcase

" # Hybrid Line Numbers

" Show current line number
set number

" Relative line numbers
set relativenumber

" # Misc

" Set the number of saved commands in history
set history=10000

" Show incomplete commands at the bottom
set showcmd

" Show Whitespace
set list
